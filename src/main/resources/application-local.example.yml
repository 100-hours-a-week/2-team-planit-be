spring: # Spring Boot 설정 루트
  datasource: # 데이터베이스 연결 정보
    url: jdbc:mysql://localhost:3306/DB_NAME # 실제 DB명으로 교체
    username: YOUR_USERNAME # DB 접속 계정
    password: YOUR_PASSWORD # DB 계정 비밀번호

jwt: # JWT 관련 설정
  secret: YOUR_JWT_SECRET # 32자 이상 시크릿으로 변경 (예: 랜덤 문자열)

planit:
  profile:
    default-image-url: https://${planit.cloudfront.domain}/default-profile.png

trip:
  create-window-enabled: false # 로컬에서는 시간 제한 비활성화
